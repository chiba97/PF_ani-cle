<div class="container admin-page">
  <%= render "user/posts/error", user: @contact %>
  <div class="row justify-content-center pt-5">
    <div class="col-sm-8">
      <h2 class="admin-title text-center"><i class="fas fa-envelope"></i> <%= @contact.user.name %>さんのお問い合わせ内容 <i class="fas fa-envelope"></i> </h2>
      <%= form_with model: @contact, url: admin_contact_path(@contact.id), method: :patch, local:true do |f| %>
      <div class="pt-3">
        <h5><i class="fas fa-paw"></i> お問い合わせ日 <i class="fas fa-paw"></i></h5>
        <p><%= @contact.created_at.strftime("%Y年%m月%d日 ") %></p>
      </div>
      <div class="pt-3">
        <h5><i class="fas fa-paw"></i> タイトル <i class="fas fa-paw"></i></h5>
        <p><%= @contact.title %></P>
      </div>
      <div class="pt-3">
        <h5><i class="fas fa-paw"></i> お問い合わせ内容 <i class="fas fa-paw"></i></h5>
        <p><%= @contact.body %></P>
      </div>
      <div class="form-group mt-4">
        <h5><i class="fas fa-paw"></i> <%= f.label :reply, "返信内容" %> <i class="fas fa-paw"></i></h5>
        <%= f.text_area :reply, class: "form-control" %>
      </div>
      <div class="form-group mt-4 mb-5">
        <%= button_tag type: "submit", class: "btn user-show-btn" do %>
        返信する <i class="far fa-paper-plane"></i>
        <% end %>
        <%= link_to admin_contacts_path, class: "btn user-show-btn ml-3" do %>
        戻る <i class="fas fa-undo-alt"></i>
        <% end %>
      </div>
      <% end %>
    </div>
  </div>
</div>