<div class="container post-page">
  <%= render "user/posts/error", user: @contact %>
  <div class="row justify-content-center">
    <div class="col-sm-6 pt-5 pb-5">
      <h3 class="post-title"><i class="fas fa-envelope"></i> お問い合わせフォーム<i class="fas fa-envelope"></i></h3>
      <%= form_with model: @contact, local:true do |f| %>
        <div class="form-group mt-5">
          <i class="fas fa-paw"></i> <%= f.label :title, "タイトル" %> <i class="fas fa-paw"></i>
          <%= f.text_field :title, class: "form-control" %>
        </div>
        <div class="form-group mt-5">
          <i class="fas fa-paw"></i> <%= f.label :body, "お問い合わせ内容" %> <i class="fas fa-paw"></i>
          <%= f.text_area :body, class: "form-control" %>
        </div>
        <div class="form-group mt-5">
          <%= button_tag type: "submit", class: "btn post-btn" do %>
          送信 <i class="far fa-paper-plane"></i>
          <% end %>
        </div>
      <% end %>
    </div>
  </div>
</div>